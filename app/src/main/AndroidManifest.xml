<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Joulare"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity
            android:name=".Activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action   android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.ParamActivity"
            android:exported="true" />

        <activity
            android:name=".Activities.LangActivity"
            android:exported="true" />

        <activity
            android:name=".Activities.RankActivity"
            android:exported="true" />

        <activity
            android:name=".Activities.NameActivity"
            android:exported="true" />

        <activity
            android:name=".Activities.GameOnActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />

        <activity
            android:name=".Activities.VictoryActivity"
            android:exported="true" />
    </application>
</manifest>